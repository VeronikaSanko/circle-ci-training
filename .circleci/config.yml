version: 2.0

jobs:
  "Flow one":
    docker: 
        - image: circleci/ruby:2.2-node
    steps:
        - checkout
        - run: echo "Hello from 1"
  "Flow two":  
    requires: 
        - "Flow one"
    docker: 
        - image: circleci/ruby:2.3-node
    steps:
        - checkout
        - run: echo "Hello from 2"  
  "Flow three":
    docker:
        - image: circleci/ruby:2.4-node
    steps:
        - checkout
        - run: echo "Cats like milk"

workflows:
  version: 2
  build:
    jobs:
      - "Flow one"
      - "Flow two"
      - "Flow three"
     
   
    
